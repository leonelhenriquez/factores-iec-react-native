import * as React from 'react';
import Icon from './Icon';

const SettingsIcon = ({ color, size, style }) => (
	<Icon name="cog" color={color} size={size} style={style} />
);

export default SettingsIcon;
